[project]
name = "smilex-mcp-server"
version = "1.0.0"
description = "SmileX MCP Server - 独立 MCP 工具服务"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "mcp[cli]>=1.0.0",
    "httpx>=0.27.0",
    "pydantic-settings>=2.12.0",
    "uvicorn[standard]>=0.40.0",
]

[dependency-groups]
dev = []
